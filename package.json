{
  "name": "driftlock",
  "version": "1.0.0",
  "description": "A configurable AI orchestrator to fight entropy and keep your projects clean, powered by OpenAI Codex.",
  "main": "dist/cli/index.js",
  "bin": {
    "driftlock": "dist/cli/index.js"
  },
  "repository": "git@github.com:glemiere/driftlock.git",
  "author": "Will Le Miere <will@lemiere.io>",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "local-install": "command -v driftlock >/dev/null 2>&1 || npm link",
    "local-uninstall": "npm uninstall -g driftlock || true",
    "dev": "npm run local-install && npm run build:watch",
    "test": "NX_DAEMON=false NX_NATIVE_LOG_PLUGIN=false NX_ADD_PLUGINS=false nx test driftlock --runInBand -- --verbose",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^22.7.4",
    "jest": "^30.2.0",
    "nx": "^22.1.3",
    "ts-jest": "^29.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "@openai/codex": "^0.72.0"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.63.0",
    "commander": "^14.0.2"
  }
}
